/**
 * Canvas Template Renderer — re-export
 * Kept for backward compatibility with existing imports.
 */
export { canvasTemplateRenderer, type CanvasRenderRequest } from './canvas-template-renderer.service'
